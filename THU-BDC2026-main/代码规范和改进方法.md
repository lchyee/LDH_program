代码规范： 选手提交的文件夹结构如下，选手需按照提供的格式组织文件，标注为必选的表示必须存在的文件或文件夹：
|--app
    |--code（储存运行代码，docker内）
        |--src
            |--featurework.py
            |--test.py
            |--train.py
    |--data（储存数据，在docker-compose.yml中进行挂载）
        |--test.csv
        |--train.csv
    |--model（储存训练模型和其它数据，docker内）
    |--output（储存计算结果，在docker-compose.yml中进行挂载）
        |--result.csv
    |--temp（储存中间结果，在docker-compose.yml中进行挂载）
    |--init.sh（docker内，必选）
    |--train.sh（docker内，必选）
    |--test.sh（docker内，必选）
    |--readme.md（必选）
选手需要按照上面所示将文件进行存储，在docker中储存code、model、init.sh、train.sh、test.sh等文件，将docker镜像命名为bdc2025，并导出为“队伍名称.tar”进行提交，我们会使用docker load -i 队伍名称.tar命令对docker镜像进行加载，并使用下发的docker-compose.yml文件运行。

修改目的：
1.我的最终目标是运用很多模型，每个模型输出自己认为的top10，然后给每一个模型预测的股票进行投票，最终输出投票结果的top5股票
2，我要在不违背代码规范的前提下，新建一个文件夹“models”，用于放所有的预测模型，且每一个模型都有它自己的文件夹，文件夹中包含模型的代码、模型的参数、模型的预测结果等。
3.所以要把原来的模型及其需要的所有文件都放到“models”文件夹里的“model01”文件夹里，而在原来的地方写投票程序，可以读取其他的模型预测的股票，然后进行投票。
4，因为模型只是输出五只股票，他们所需要的数据和判断股票得分的程序是一样的,所有这些东西要放在“models”外，